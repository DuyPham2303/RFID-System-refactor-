#include "Spi_Internal.h"

/**
 * @brief Bảng callback notification của các SPI Job.
 * @details Mỗi phần tử tương ứng với một Job ID logic và được gọi khi Job
 *          tương ứng hoàn tất, nếu callback đã được đăng ký.
 */
// static Spi_NotificationType Spi_JobCallbacks[];

/**
 * @brief Bảng callback notification của các SPI Sequence.
 * @details Mỗi phần tử tương ứng với một Sequence ID logic và được gọi sau
 *          khi toàn bộ Job thuộc Sequence đã thực thi xong.
 */
// static Spi_NotificationType Spi_SequenceCallbacks[];
